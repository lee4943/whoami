const APP_PORT = 80;
const CLIENT_ID = "";
const CLIENT_SECRET = "";
const DB_SERVER = "localhost";
const DB_PORT = 27017;
const DB_NAME = "whoami";

module.exports = {
    APP_PORT: APP_PORT,
    CLIENT_ID: CLIENT_ID,
    CLIENT_SECRET: CLIENT_SECRET,
    DB_SERVER: DB_SERVER,
    DB_PORT: DB_PORT,
    DB_NAME: DB_NAME
};